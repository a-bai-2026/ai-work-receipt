# Contributing

欢迎提交解析兼容、视觉主题、测试与文档改进。

## 本地验证

```bash
npm install
npm test
npm run check
npm run receipt -- --latest --no-open
```

## 隐私要求

- 不要提交真实 Codex 会话文件。
- 测试数据必须是人工构造或彻底脱敏的 fixture。
- 新增指标不得把 Prompt、回复正文、代码、项目路径或文件名写入输出。

## 主题贡献

主题应只改变视觉表达，不改变统计口径。请同时提供桌面窄屏截图，并确认二维码在主题下仍可扫描。

