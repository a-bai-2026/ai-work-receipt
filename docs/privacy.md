# Privacy model

默认行为：

- 只读取 `~/.codex/sessions/**/*.jsonl`
- 只提取事件类型、时间和数字统计
- 不保存 Prompt、回复正文、代码、项目路径或文件名
- 不向网络发送数据
- 结构数据保存在 `~/.codex-work-receipt/`

二维码只包含当前小票的脱敏统计。二维码等同于一份可扫描的数据文件，请只向信任的人展示。

